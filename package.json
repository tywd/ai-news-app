{
  "name": "ai-news-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5175",
    "build": "vite build",
    "build:check": "vue-tsc -b && vite build",
    "preview": "vite preview --port 8889 --host",
    "start": "node server.js",
    "deploy": "vercel --prod",
    "serve": "node server.js",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build"
  },
  "dependencies": {
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@vitejs/plugin-vue": "^6.0.1",
    "@vue/tsconfig": "^0.7.0",
    "typescript": "~5.8.3",
    "vite": "^7.1.2",
    "vite-plugin-qiankun": "^1.0.15",
    "vue-tsc": "^3.0.5"
  }
}
